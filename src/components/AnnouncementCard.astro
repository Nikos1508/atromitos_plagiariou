---
const { title, text } = Astro.props;
---

<div class="max-w-md bg-white p-6 rounded-lg shadow-lg transition transform hover:scale-105 hover:shadow-2xl">
    <h2 class="text-2xl font-semibold text-blue-900 mb-2 text-center">{title}</h2>
    <p class="text-gray-700">{text}</p>
</div>